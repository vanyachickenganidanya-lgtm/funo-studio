{
  "name": "funo-studio",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "node scripts/prepare-sidecar.mjs && tauri build",
    "android:init": "tauri android init --ci",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build --apk --ci",
    "android:build:debug": "tauri android build --debug --apk --ci"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "monaco-editor": "^0.52.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.1"
  }
}
